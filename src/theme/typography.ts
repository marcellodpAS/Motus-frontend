import tokens from "./tokens";

/** Temporary typography scale, pending the Stitch token review. */
export const typography = tokens.typography;

export type TypographyToken = keyof typeof typography;
