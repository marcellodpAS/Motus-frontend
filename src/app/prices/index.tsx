import { PricesSearchScreen } from "@/features/prices-search/PricesSearchScreen";

/** Route: composes the S02 feature screen. No shared components or business logic here (architecture.md §2). */
export default function PricesRoute() {
  return <PricesSearchScreen />;
}
