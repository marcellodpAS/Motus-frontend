import { ProfileScreen } from "@/features/profile/ProfileScreen";

/** Route: tab "Profile" (Task 19). No shared components or business logic here (architecture.md §2). */
export default function ProfileRoute() {
  return <ProfileScreen />;
}
