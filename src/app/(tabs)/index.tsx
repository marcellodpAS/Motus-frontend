import { MapScreen } from "@/features/map/MapScreen";

/** Route: tab "Map", the app's home screen (Task 19). No shared components or business logic here (architecture.md §2). */
export default function MapRoute() {
  return <MapScreen />;
}
