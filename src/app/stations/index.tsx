import { StationsSearchScreen } from "@/features/stations-search/StationsSearchScreen";

/** Route: composes the S01 feature screen. No shared components or business logic here (architecture.md §2). */
export default function StationsRoute() {
  return <StationsSearchScreen />;
}
